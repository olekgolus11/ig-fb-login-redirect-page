export interface ShortLivedAccessTokenData {
    access_token: string;
    token_type: string;
    expires_in: number;
}

export interface LongLivedAccessTokenData {
    access_token: string;
    token_type: string;
    expires_in: number;
}
